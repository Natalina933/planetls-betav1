import type { BusinessScores, BusinessStrategy } from "./types";
import styles from "./BusinessStrategyCenter.module.scss";
const labels: Record<keyof BusinessScores,string>={technicalComplexity:"Complexité technique",commercialClarity:"Lisibilité commerciale",revenuePotential:"Potentiel de revenus",salesEase:"Facilité de vente",maintenanceCost:"Coût de maintenance",scalability:"Évolutivité",risk:"Risques"};
export function BusinessScore({strategy,onChange}:{strategy:BusinessStrategy;onChange:(s:BusinessStrategy)=>void}){return <section className={styles.module}><div className={styles.moduleHeader}><div><span>Analyse structurée, sans IA</span><h2>Scores du modèle</h2></div></div><div className={styles.scoreGrid}>{(Object.keys(labels) as (keyof BusinessScores)[]).map((key)=><label key={key}><span>{labels[key]} <strong>{strategy.scores[key]}/100</strong></span><input type="range" min="0" max="100" value={strategy.scores[key]} onChange={(e)=>onChange({...strategy,scores:{...strategy.scores,[key]:Number(e.target.value)}})}/></label>)}</div><p className={styles.aiNote}>Architecture prête pour une recommandation IA future : aucune IA n’est connectée et aucun score n’est présenté comme une vérité.</p></section>}