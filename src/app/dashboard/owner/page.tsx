// src/app/(dashboard)/owner/page.tsx

// Importation de React n'est pas nécessaire mais peut aider si vous utilisez des hooks.
import React from 'react'; 

// C'est le composant le plus simple possible.
export default function MinimalOwnerPage() {
    return (
        <div>
            <h1>Succès: Dashboard Propriétaire Minimal</h1>
        </div>
    );
}

// Retirez tous les autres imports, console.log ou lignes de code.