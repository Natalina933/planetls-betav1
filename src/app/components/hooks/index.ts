// Hooks réutilisables
// ============================================================

export { useCurrentUser } from "./useCurrentUser";
export { useAutoOpen } from "./useAutoOpen";
export { useInactivityLogout } from "./useInactivityLogout";

// Animation hooks (PHASE 2)
export {
  useReveal,
  useSimpleReveal,
  useStaggeredReveal,
  useParallax,
  useReducedMotion,
} from "./useReveal";
