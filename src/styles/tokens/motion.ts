/** Références CSS : les valeurs restent définies dans tokens.css. */
export const motion = {
  instant: "var(--ds-motion-instant)",
  fast: "var(--ds-motion-fast)",
  normal: "var(--ds-motion-normal)",
  slow: "var(--ds-motion-slow)",
  easing: "var(--ds-ease-premium)",
} as const;
